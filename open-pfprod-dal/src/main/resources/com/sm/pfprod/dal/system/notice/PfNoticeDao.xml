<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sm.pfprod.dal.system.notice.PfNoticeDao">

    <sql id="Base_Column_List" >
        notice_title, notice_type, notice_content, is_deleted, creator, gmt_create, gmt_modify
    </sql>

    <!-- 获取公告list -->
    <select id="listNotices" parameterType="com.sm.pfprod.model.dto.system.notice.PfNoticeDto"
            resultType="com.sm.pfprod.model.vo.system.SysNoticeVo">
        SELECT
          sn.id, sn.notice_title, sn.notice_type, sn.notice_content, sn.creator, sn.gmt_create,
          ui.username as creatorName
        FROM
          sys_notice sn
        INNER JOIN user_info ui on sn.creator = ui.user_id
        WHERE
          sn.is_deleted = 'N'
        <if test="publishTimeBegin != null and publishTimeBegin != '' ">
            AND sn.gmt_create >= #{publishTimeBegin}
        </if>
        <if test="publishTimeEnd != null and publishTimeEnd != '' ">
            <![CDATA[
            AND sn.gmt_create <=  #{publishTimeEnd}
           ]]>
        </if>
        ORDER BY sn.id desc
        limit #{offset}, #{limit}
    </select>

    <!-- 获取公告总数 -->
    <select id="countNotices" parameterType="com.sm.pfprod.model.dto.system.notice.PfNoticeDto"
            resultType="java.lang.Long">
        SELECT count(1) FROM sys_notice
        WHERE is_deleted = 'N'
        <if test="publishTimeBegin != null and publishTimeBegin != '' ">
            AND gmt_create >= #{publishTimeBegin}
        </if>
        <if test="publishTimeEnd != null and publishTimeEnd != '' ">
            <![CDATA[
            AND gmt_create <=  #{publishTimeEnd}
           ]]>
        </if>
    </select>

    <!-- 新增公告 -->
    <insert id="addNotice" parameterType="com.sm.pfprod.model.entity.SysNotice">
        INSERT INTO sys_notice(
        <include refid="Base_Column_List"/>
        )
        VALUES(
        #{noticeTitle} , #{noticeType} , #{noticeContent} , 'N' , #{creator} , NOW() , NOW()
        );
    </insert>

    <!-- 编辑公告 -->
    <update id="editNotice" parameterType="com.sm.pfprod.model.entity.SysNotice">
        UPDATE sys_notice
        SET
        <if test="noticeTitle != null">
            notice_title = #{noticeTitle} ,
        </if>
        <if test="noticeType != null">
            notice_Type = #{noticeType} ,
        </if>
        <if test="noticeContent != null">
            notice_content = #{noticeContent} ,
        </if>
        gmt_modify = NOW()
        WHERE
        id = #{id}
    </update>

    <!-- 删除公告 -->
    <delete id="delNotice" parameterType="java.util.List">
        UPDATE sys_notice SET is_deleted = 'Y'
        WHERE
        id in (
        <foreach collection="list" item="item" separator="," >
            #{item}
        </foreach>
        )
    </delete>

</mapper>